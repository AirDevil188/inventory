extends layout

block content

    if genre_list 
        h2.title List of Genres:
        ul
            each genre in genre_list
                li
                    a(href=`${genre.url}`) !{genre.name}
                   
                    
    else 
        p There are no genres.