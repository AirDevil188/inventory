extends layout

block content

    if publisher_list 
        h2.title List of Publishers:
        ul
            each publisher in publisher_list
                li
                    a(href=`${publisher.url}`) !{publisher.name}
                   
                    
    else 
        p There are no publishers.

        
        
        
