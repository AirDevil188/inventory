extends layout

block content
    
        form(method="POST")
            div.form-group
                label(for="platform_name").form-label #[strong Name: ]
                input(type="text" id="platform_name" name="platform_name").form-control

            div.form-group
                    button.btn.btn-primary(type="submit") Submit